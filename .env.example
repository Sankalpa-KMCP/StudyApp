# Study Dashboard — environment variables

# Normal local development does not require a `.env` file.
# Copy this file only if you later introduce an optional tool that needs configuration.
#
# There are currently no application or CI environment variables required by this repository.
